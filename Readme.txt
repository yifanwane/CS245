This program implements a shifted binary search algorithm to find the index of a target integer in a shifted sorted array. 
The input array is assumed to be initially sorted, 
but it may have been shifted (rotated) at some point,
and we need to efficiently find the target element's index.

Yifan Wan
9/15/2023